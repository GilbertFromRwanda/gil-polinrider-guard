// benign fixture for polinrider-guard testing
// contains inert Glassworm-style invisible Unicode for detection testing only
function ping(x) { // marker​
  const label = "ok︀";
  return label + x;
}

module.exports = { ping };
