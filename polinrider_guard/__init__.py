"""polinrider-guard: detection tooling for the PolinRider supply-chain campaign.

Four independent scanners (invisible Unicode, file-extension masquerade,
risky VS Code tasks, git commit-date backdating) plus a `guard` aggregator
that runs all of them and produces a single structured report.
"""

__version__ = "0.1.0"
